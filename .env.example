# Required only when connecting the Prisma API route to a MySQL database.
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/amr_hub"

# Public production origin used for social sharing metadata.
NEXT_PUBLIC_APP_URL="https://your-domain.example"
